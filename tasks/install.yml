---

- name: Adding sources
  shell: curl -s https://packagecloud.io/install/repositories/foomo/content-server/script.deb.sh | bash
  args:
    creates: /etc/apt/sources.list.d/foomo_content-server.list

- name: Installing package
  action: "{{ ansible_pkg_mgr }} pkg={{ content_server_package }} state=present"
